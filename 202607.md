# スライド1

OpenID Foundation Japanが開催した、「ID沼入口――基本とセキュリティから始める、考え続けるためのID管理技術勉強会」に参加しました。

本日は、勉強会で行われた3つの講演について、どのような内容だったのかを紹介します。

そのうえで、認証基盤の設計やレビューに、どのような観点を持ち帰りたいと感じたのかを報告します。

# スライド2

本日の流れです。

最初のritou氏の講演では、ID管理を認証やログインだけで捉えるのではなく、構成要素やユーザーのライフサイクルを含めて整理する考え方が紹介されました。

次の徳丸氏の講演では、セッション管理を題材として、タイムアウトの設計やXSS、CSRFなど、Webセキュリティ全体を俯瞰していました。

最後のかろっく氏の講演では、OAuthやOpenID Connectを学ぶときに生じる「わからない」を、どのように分解して解消していくかが紹介されました。

この順番で講演内容を紹介し、最後に3つの講演を通して持ち帰りたいことをまとめます。

# スライド3

まず、ritou氏の講演です。

この講演では、ID管理を5つの構成要素に分けて説明していました。

身元確認、当人認証、ID連携、アクセス制御、セッション管理の5つです。

身元確認では、サービスが必要とする属性情報を収集し、その情報が利用者のものかを確認します。

当人認証では、登録された本人が実際にサービスを利用していることを、パスワードなどの認証情報によって確認します。

アクセス制御では、その利用者がどのデータに対して、どのような操作を行えるのかを決めます。

そしてセッション管理では、一度認証された状態を一定期間維持します。

ID連携は、こうした身元確認や当人認証の一部を、信頼する外部のIdPなどと分担する仕組みとして整理されていました。

ここで重要だと感じたのは、ID管理はログイン処理やOIDCのフローだけではないという点です。

利用者への信頼を作り、アクセスを許可し、その状態を維持するところまで含めて考える必要があります。

では、そのID管理をサービスの運用に当てはめると、どのような機能が必要になるのか。

そこで出てくるのが、次のライフサイクルの考え方です。

# スライド4

講演では、ユーザーがサービスを利用し始めてから、利用を終えるまでの状態変化が紹介されていました。

たとえば、未登録のユーザーが本登録になるためには、新規登録と属性情報の確認が必要です。

本登録された後も、ユーザー情報が変更される場合があります。その内容によっては、変更した属性を改めて確認する必要もあります。

また、何らかの理由でサービスを利用できない状態にする場合には、本登録から無効化へ状態を変更します。

利用そのものを終了する場合には、退会処理が必要になります。

このようにユーザーの状態を見ることで、ログイン以外にも、情報変更、利用停止、退会などの機能が必要だと分かります。

講演では、こうした変更を後から追えるように、セキュリティイベントとして記録する観点も紹介されていました。

このように、まず単体サービスに必要なID管理機能を整理します。

そのうえでID連携を利用する場合には、この中の何を自分たちで管理し、何をIdPへ委譲するのかを判断します。

OIDCという仕組みだけを見るのではなく、委譲する前のID管理機能を整理しておくことで、機能の不足や実装・運用コストを判断しやすくなる、という内容でした。

ここまでが、ユーザーやIDをどのように管理するかという話です。

次の徳丸氏の講演では、その構成要素の一つであるセッション管理を切り口に、Webセキュリティ全体を見ていきます。

# スライド5

徳丸氏の講演では、セッション管理はWebセキュリティの根幹となる重要な要素だと説明されていました。

セッション管理は、ログイン状態を維持する仕組みだけではありません。

開始、維持、終了という一連の流れを設計する必要があります。

まず開始では、ログインによってセッションが作られます。

このとき、同じユーザーによる複数端末からのログインを許可するのかを決めます。

許可しない場合も、先に存在するセッションを残すのか、後からログインしたセッションを残すのかといった方針が必要です。

次に維持です。

よく使われるのが、操作するたびに有効期限を延長するスライディングセッションです。

利用者が操作している間はログイン状態を保てるため、利便性の高い方式です。

一方で、操作が続けば有効期限も延び続けます。

仮にセッションが攻撃者に利用された場合、攻撃者が操作を続けることで、セッションを長時間維持できる可能性があります。

そこで、ログインから一定時間が経過したら、操作の有無に関係なく終了させる絶対タイムアウトを組み合わせます。

最後が終了です。

利用者がログアウトした場合や、タイムアウトに達した場合には、ブラウザ側だけでなく、サーバー側でもセッションを確実に無効化する必要があります。

つまりセッション管理では、ログイン状態をどう維持するかだけでなく、どの条件で必ず終了させるかまで決めることが重要です。

ただし、セッションを適切に管理していても、ブラウザ上で攻撃者のスクリプトが実行されたり、意図しないリクエストを送られたりする可能性は残ります。

そこで講演では、XSSやCSRFとセッション管理の関係についても説明されていました。

# スライド6

まずXSSです。

XSSがあると、攻撃者が用意したJavaScriptが、利用者のブラウザ上で実行されます。

CookieにHttpOnlyが付いていない場合は、JavaScriptからCookie値を読み取られ、セッションIDを盗まれる可能性があります。

HttpOnlyを付けることで、Cookie値をJavaScriptから直接取得されることは防げます。

ただし、HttpOnlyを付けても、悪意のあるJavaScriptそのものが動かなくなるわけではありません。

スクリプトからAPIを呼び出した場合、ブラウザはCookieを自動的に送信します。

そのため、Cookie値を盗めなくても、利用者として情報を取得したり、操作を実行したりできる可能性があります。

つまり、HttpOnlyは必要な対策ですが、それだけでXSSによる被害を防げるわけではありません。

CSPなども含めて、攻撃者のスクリプトを実行させない対策が必要になります。

次にCSRFです。

CSRFは、ブラウザがCookieを自動的に送信する性質を利用して、利用者が意図していない操作を実行させる攻撃です。

SameSiteを設定すると、クロスサイトからのCookie送信を制限できるため、一般的なCSRFを軽減できます。

一方で講演では、サブドメインテイクオーバーなど、ブラウザから同一サイトと判断されるケースでは、SameSiteだけでは限界があると説明されていました。

そのため、重要な操作ではCSRFトークンなど、サーバー側で正規のリクエストかを検証する仕組みも必要になります。

また、LocalStorageにBearer Tokenを保存する構成では、XSSによってトークンそのものを読み取られる可能性があります。

その対策の選択肢として、BFFを利用し、トークンをブラウザではなくサーバー側に保持する構成も紹介されていました。

この講演を通して、個別の設定名を覚えるだけではなく、その対策で何を防げて、何がまだ残るのかを考えることが重要だと感じました。

ここまでの2つの講演は、ID管理やセキュリティをどう設計するかという話でした。

最後の講演では、こうした複雑な認証認可の内容を、そもそもどのように学べばよいのかが扱われました。

# スライド7

かろっく氏の講演は、認証認可を通して「わからない」をどのように潰すかという内容でした。

OAuthやOIDCは、登場人物が多く、用語同士も互いに前提となっています。

ある用語を調べると、説明の中に別の知らない用語が現れ、その用語を調べると最初の用語へ戻ってくる。

このような循環参照が、認証認可を分かりにくくする要因として紹介されていました。

そこで、最初からすべてを理解しようとするのではなく、まず登場人物を固定します。

OAuthであれば、利用者であるResource Owner、アプリケーションであるClient、認可サーバーであるAuthorization Server、APIを提供するResource Serverです。

そのうえで、まず認可コードフローのような基本的な一本道を追います。

誰が、誰に、何を渡しているのかを確認しながら、全体の流れを理解します。

次に、攻撃と防御をセットで確認します。

どのような問題があり、その対策としてなぜ仕組みが追加されたのかを見ることで、仕様の意味を理解します。

基本的な流れと、その仕組みが必要になった理由を理解した後に、Public Clientやほかのフロー、OIDCへ差分を広げていきます。

自分自身も、分からないまま仕様書を読み続けると、どこが分からないのかまで曖昧になることがあります。

そのため、登場人物と目的を固定し、分からない箇所を具体的な質問に変換するという方法は、今後の学習にも活かせると感じました。

以上が3つの講演内容です。

最後に、それぞれの講演から業務へ持ち帰りたいことをまとめます。

# スライド8

今回の持ち帰りは3点です。

1つ目は、ID管理をライフサイクル全体で捉えることです。

認証処理だけでなく、ユーザーの登録、情報変更、利用停止、退会までを見ることで、必要な機能や状態管理の不足に気付きやすくなります。

2つ目は、セッション管理を終了まで含めて設計することです。

ログイン状態を維持する方法だけでなく、同時ログインの方針、タイムアウト、ログアウト時の無効化まで、サービスのポリシーとして決める必要があります。

また、セキュリティ対策についても、設定して終わりではなく、何を防げて何が残るのかまで確認したいと考えています。

3つ目は、分からない概念を分解することです。

仕様を読む際には、まず登場人物と目的を固定し、基本的なフローから理解します。

そのうえで、攻撃と防御、別のフロー、OIDCとの差分へと段階的に範囲を広げます。

今後は、OIDCの認証フローだけを見るのではなく、その前後にあるID管理、セッション管理、セキュリティ、運用まで含めて、設計やレビューに取り組みたいと思います。

以上です。
