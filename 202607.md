# スライド1

OpenID Foundation Japanが開催した、「ID沼入口」基本とセキュリティから始める、考え続けるためのID管理技術勉強会に参加しました。

本日は、勉強会で行われた3つの講演について、講演内容と、業務へ持ち帰りたいと考えた点を報告します。

# スライド2

本日の流れです。

今回の勉強会では、大きく3つの講演がありました。

ritou氏の講演は、ID管理を単なる認証・認可の機能として捉えるのではなく、身元確認、当人認証、ID連携、アクセス制御、セッション管理といった構成要素や、ユーザーのライフサイクル全体から整理する内容です。

徳丸氏の講演は、セッション管理を題材にして、タイムアウトの設計、Cookieの扱い、XSSやCSRF、CSPなど、Webセキュリティ全体を俯瞰する内容です。

かろっく氏の講演は、OAuthやOpenID Connectを学ぶ際に起きやすい、用語や仕様の循環参照による「わからなさ」に対して、どのように内容を分解し、理解を進めていくかという内容です。

この報告では、まず各講演で何が話されたのかを紹介します。そのうえで、最後に自分が業務へ持ち帰りたい観点をまとめます。

# スライド3

ritou氏の講演では、ID管理を5つの構成要素に分けて説明していました。

身元確認、当人認証、ID連携、アクセス制御、セッション管理です。

ここで印象に残ったのは、ID管理はログイン機能だけを指すものではないという点です。

利用者についての信頼をどう作るか、本人であることをどう確認するか、何を許可するか、認証済みの状態をどう維持するかまで含めて整理されていました。

さらにID連携は、こうした機能の一部を外部のIdPや他サービスと分担する仕組みとして位置づけられていました。

そのため、OIDCやプロトコルから考え始めるだけではなく、まず単体サービスとして必要なID管理機能を分解することが重要だと理解しました。

# スライド4

次に、ID管理をライフサイクルで見る話です。

講演では、ユーザーの状態と、その状態が変わる際に必要となる機能が紹介されていました。

たとえば、未登録から本登録へ進むには、新規登録や属性情報の確認が必要になります。

本登録のユーザーが登録情報を変更する場合は、ユーザー情報変更の機能が必要です。内容によっては、属性情報を再検証する必要もあります。

また、利用停止する場合は、本登録から無効化へ状態を変える機能が必要です。退会する場合には、退会処理が必要になります。

補足として、こうした状態変更は、後から追跡できるようにセキュリティイベントとして記録する観点も紹介されていました。

そのうえでID連携を使う場合には、何を自分たちで管理し、何をIdPへ委譲するかを考えることになります。

# スライド5

徳丸氏の講演では、セッション管理がWebセキュリティの根幹として位置づけられていました。

このスライドでは、セッションを開始、維持、終了の3段階で整理しています。

開始では、ログインによってセッションが作られます。

このとき、同時ログインを許可するのか、許可しない場合は先にログインしていたセッションを残すのか、後からログインしたセッションを残すのかといった方針を、サービスの利用場面に応じて決める必要があります。

維持では、認証済みの状態を一定期間保ちます。

講演では、操作するたびに有効期限が延びるスライディングセッションが説明されました。

ただし、スライディングセッションだけでは、操作が続く限り有効期限も延び続けます。セッションが攻撃者に使われた場合も同じです。

そこで終了の設計として、ログインから一定時間が経過したら、操作の有無に関係なく終了させる絶対タイムアウトを組み合わせます。

ログアウト時やタイムアウト時に、サーバー側でセッションを確実に無効化することも重要です。

つまり、セッション管理は単なるCookieの設定ではありません。

ログイン後の状態をどう維持し、どの条件で必ず終了させるかを、ポリシーとして決める設計だと理解しました。

# スライド6

このスライドでは、徳丸氏の講演で扱われたXSSとCSRFを、攻撃されたときに何が起きるかという点から整理します。

まずXSSです。

XSSがあると、攻撃者のJavaScriptが利用者のブラウザ上で実行されます。

CookieにHttpOnlyを付けていない場合は、Cookie値そのものを読み取られ、セッションIDを盗まれる可能性があります。

HttpOnlyを付けることで、JavaScriptからCookie値を直接取得されることは防げます。

ただし、HttpOnlyを付けていても、悪意のあるJavaScriptからAPIを呼び出すことはできます。

その際、ブラウザはCookieを自動的に送信するため、利用者として操作される可能性は残ります。

つまり、HttpOnlyは必要な対策ですが、XSS自体を防ぐものではありません。

CSPなども含めて、不正なスクリプトを実行させないための対策が必要になります。

次にCSRFです。

CSRFは、利用者のCookieが自動的に送信される性質を利用して、利用者が意図していない操作を実行させる攻撃です。

SameSiteは、クロスサイトからのCookie送信を制限するため、一般的なCSRFを軽減できます。

一方で講演では、サブドメインテイクオーバーのように同一サイト扱いになるケースでは、SameSiteだけでは限界があると説明されていました。

そのため、CSRFトークンのように、サーバー側でリクエストを検証できる仕組みも併用する必要があります。

また、LocalStorageにBearer Tokenを置く構成では、XSSが発生した際にトークンを読み取られるリスクがあります。

BFFなどを使って、ブラウザ側にトークンを置かない設計も選択肢として紹介されていました。

この講演で重要だと感じたのは、設定名を覚えることではなく、攻撃されたときに何を防げて、どのリスクが残るのかを分けて考える点です。

# スライド7

かろっく氏の講演は、認証認可を通して「わからない」をどう潰すかという内容でした。

OAuthやOIDCは登場人物が多く、用語同士も互いに前提になりやすいため、全体を一度に理解しようとすると、循環参照のように感じやすいと整理されていました。

OAuthの登場人物としては、利用者であるResource Owner、アプリケーションであるClient、認可サーバーであるAuthorization Server、APIや保護されたリソースを持つResource Serverがあります。

講演では、この分かりにくさに対して、いきなりOIDC全体を見るのではなく、まず登場人物を固定し、認可コードフローという素直な一本道を通して追う、という順序が紹介されました。

その後で、攻撃と防御をセットで見て、「なぜその仕組みが必要なのか」を理解します。

そこからPublic Clientやほかのフロー、OIDCへ、差分として理解を広げていくという流れです。

自分の学習経験を振り返っても、分からない状態のまま仕様を読み進めるより、まず登場人物と目的を固定し、モヤモヤを具体的な質問に変換することが重要だと感じました。

# スライド8

最後に、全体を通して持ち帰りたいことを3点にまとめます。

1つ目は、ID管理を状態遷移から見ることです。

ユーザーが登録され、情報が変わり、停止や退会に進む。その変化を見ることで、必要な機能を考えやすくなると感じました。

2つ目は、セッション管理を「維持」だけでなく「終了」まで含めて設計することです。

ログイン後の状態をどう保つかだけではなく、どの条件で必ず終了させるかまで、ポリシーとして決める必要があります。

3つ目は、分からない概念を分解して、具体的な質問にすることです。

OAuthやOIDCのように登場人物や用語が多い領域では、まず登場人物と目的を固定し、どこが分からないのかを具体化することが重要だと感じました。

今後は、OIDCの認証フローだけでなく、その前後にあるID管理、セッション管理、運用まで含めて、設計やレビューに取り組みたいと思います。

以上です。
